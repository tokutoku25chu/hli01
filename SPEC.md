# 📷 HLI 商品画像後処理ツール 仕様書

## 1. プロジェクト概要

### 目的
ECサイト「HLI」（リネン製品・寝具・ウェア・小物などを扱う）の商品画像処理を自動化する。

カメラマンから届いた撮影画像（3:2横、未クロップ）を入力として、以下を一括処理する：

1. カテゴリ別の構図ルールに従ってクロップ
2. 6パターンのサイズへリサイズ
3. WebP変換による軽量化（高画質フォルダはJPG/PNG維持）
4. 寝具系カテゴリのみサイズバッジを自動付与
5. Dropbox上の規定フォルダへ自動保存

### 想定処理量
- 1回あたり100枚程度
- 11カテゴリへの自動振り分け対応

### 開発方針
v1（コア機能）→ v2（Claude Vision API進化）の段階開発。
**まずv1を完成させてから v2 に進む。一度に全部実装しない。**

---

## 2. 入力仕様

### 2.1 入力画像
- 配置場所：`input/` フォルダ
- 形式：JPG または PNG
- 縦横比：3:2 横（一眼レフ撮影想定）
- ファイル名：商品ID（拡張子なしの部分）

### 2.2 商品リスト CSV
- ファイル名：`size_list.csv`（名称は据え置き。中身は「商品リスト」として機能拡張）
- 文字コード：UTF-8
- 列構成（v1.2 で「新ファイル名」列を追加。後方互換で 2 列形式も読める）：

| 列名 | 必須 | 内容 | 例 |
|---|---|---|---|
| 商品ID | ✅ | 入力ファイル名 stem。末尾の「のコピー」「のコピー2」等は自動で無視 | hli_240802_051 |
| 新ファイル名 | 任意 | 出力時のファイル名 stem。空欄なら元のファイル名を使用 | mrblkt_navy_1 |
| サイズコード | 任意 | 下記のいずれか。空欄ならバッジなし | S |

### 2.3 有効なサイズコード

| コード | 意味 |
|---|---|
| S | シングル |
| SD | セミダブル |
| D | ダブル |
| WD | ワイドダブル |
| Q | クイーン |
| K | キング |
| USK | USキング |

---

## 3. 実行時の対話

スクリプト実行時、ターミナルで以下を順に聞く：

### 3.1 カテゴリ選択（番号入力）

```
カテゴリを選んでください:
   1.  01_リネン            🏷️バッジあり
   2.  02_ウェア
   3.  03_小物
   4.  04_タオル・ローブ
   5.  05_パッケージ
   6.  06_ギフト
   7.  10_ノベルティー
   8.  11_シモンズ          🏷️バッジあり
   9.  12_シルク真綿布団     🏷️バッジあり
   10. 13_ダウン・フェザー製品 🏷️バッジあり
   11. 14_ブランケット       🏷️バッジあり
> 
```

### 3.2 シリーズ名（自由入力）
シリーズ名は都度変わる。ユーザーが任意の文字列で入力。

例：「2025春コレクション」「ホワイトリネンシリーズ」

---

## 4. クロップ処理（v1：シンプル自動）

### 4.1 カテゴリ別 クロップ中心位置（v1.3 改訂）

#### ベッド系：サムネイルのみ左カット
**サムネイル（1200×1200、バッジ対象）のみ** 中心を右に15%シフト。それ以外の出力（1200×1800、通常の1200×1200、スライダーPC・スマホ）は中央クロップ。

- 01_リネン
- 11_シモンズ
- 12_シルク真綿布団
- 13_ダウン・フェザー製品
- 14_ブランケット

理由：商品一覧で並ぶサムネイルではベッドの柄や縁取りを強調するため左側を意図的にカット。詳細ページ等の他サイズはベッド全体を見せたいため中央配置。

#### 中央クロップ（全出力で中央配置）

- 02_ウェア
- 03_小物
- 04_タオル・ローブ
- 05_パッケージ
- 06_ギフト
- 10_ノベルティー

### 4.2 出力サイズへのクロップ

入力（3:2横）から、上記中心位置を基準に各出力サイズにクロップする：

| 出力サイズ | 比率 | クロップ方法 |
|---|---|---|
| 1200×1200 | 1:1 | 正方形クロップ |
| 1200×1800 | 2:3 縦 | 縦長クロップ |
| 1922×824 | 約2.33:1 | 横長クロップ（上下カット） |
| 641×854 | 約3:4 縦 | 縦長クロップ |

### 4.3 リサイズ品質
- リサンプリング：Lanczos（高品質）
- アスペクト比は保ったまま指定サイズに収める
- 必要に応じて拡大も行う

---

## 5. 出力フォルダ構造

### 5.1 保存先ベースパス
```
~/Dropbox/03_HLIデザイン/03_素材写真/■商品カテゴリ/
```

### 5.2 完全な階層

```
~/Dropbox/03_HLIデザイン/03_素材写真/■商品カテゴリ/
└─ {選択カテゴリ}/
   └─ {シリーズ名}/
      ├─ 高画質（印刷用・バナー作成用）/  📄 元画像JPG/PNGをそのままコピー（クロップなし）
      ├─ スライダー用/
      │   ├─ PC/                       📄 1922×824 WebP
      │   └─ スマホ/                    📄 641×854 WebP
      ├─ サムネイル/                    📄 1200×1200 WebP（バッジ対象はバッジ付き）
      ├─ psd/                          📂 空フォルダ
      ├─ 1200×1800/                    📄 WebP（バッジなし）
      └─ 1200×1200/                    📄 WebP（バッジなし）
```

### 5.3 重要な注意点
- フォルダ名に **日本語・全角記号（■、×、・）** を含むため UTF-8 で正しく扱うこと
- macOS / Windows 両対応
- 既存フォルダがあっても上書き保存OK
- Dropboxパスが存在しない場合は警告を出し、「続行 / 中止」をユーザーに確認

---

## 6. サイズバッジ仕様

### 6.1 適用条件
**すべての条件を満たす場合のみ** バッジを描画：

1. カテゴリが以下のいずれか：
   - 01_リネン
   - 11_シモンズ
   - 12_シルク真綿布団
   - 13_ダウン・フェザー製品
   - 14_ブランケット
2. `size_list.csv` に該当商品IDが存在
3. 出力ファイルが **1200×1200 のサムネイル**

→ それ以外（1200×1800、スライダー用など）にはバッジを入れない。

### 6.2 バッジデザイン

| 要素 | 仕様 |
|---|---|
| 形状 | 円形 |
| 直径 | 140px |
| 背景色 | クリーム/ベージュ #C9B89C |
| 背景透過 | 70% |
| 文字色 | 白 |
| フォント | セリフ系 |
| 文字サイズ | バッジ直径の50%程度 |
| 装飾 | 文字上に小さなフローリッシュ（◇や細いライン） |
| 位置 | 画像右下から余白50px |
| アンチエイリアス | 有効（綺麗な円を描画） |

### 6.3 文字サイズ調整
- USKは3文字あるため少し小さめに自動調整

---

## 7. WebP変換設定

| 出力先 | 形式 | 品質 |
|---|---|---|
| 高画質（印刷用・バナー作成用） | **JPG/PNGのまま**（変換しない） | 元画像維持 |
| スライダー用（PC・スマホ） | WebP | quality=85 |
| サムネイル | WebP | quality=90（バッジが綺麗に見える） |
| 1200×1800 | WebP | quality=85 |
| 1200×1200 | WebP | quality=85 |

---

## 8. エラー処理・ロギング

### 8.1 進捗表示
- `tqdm` ライブラリで進捗バーを表示
- 「現在何枚目を処理中か」がわかるようにする

### 8.2 エラーハンドリング
- エラーが発生した画像はスキップして処理続行
- `error_log.txt` に記録（タイムスタンプ、商品ID、エラー内容）

### 8.3 処理サマリー
処理終了時に以下を表示：
- 総処理件数
- 成功件数（フォルダ別）
- バッジ追加件数
- エラー件数
- 処理時間

### 8.4 日本語フォント検出
- システムフォントを自動検出（Hiragino Sans, Yu Gothic等）
- 見つからない場合は親切なエラーメッセージで案内

---

## 9. 開発フェーズ

### Phase 1（v1）：コア機能 ← まずこれを完成させる
- カテゴリ選択UI
- 入力画像読み込み
- カテゴリ別固定ルールクロップ
- 6サイズへのリサイズ
- サイズバッジ描画
- WebP変換
- Dropboxフォルダ構造への保存
- エラーログ
- 進捗バー

### Phase 2（v2）：Claude Vision API 進化
- 環境変数 `ANTHROPIC_API_KEY` から認証
- 各画像をClaude Vision API（`claude-sonnet-4-5`）に送信
- 画像ごとに最適なクロップ座標を取得
- 固定ルール（左15%シフト等）の代替として使用
- API送信前にダウンサイズしてコスト削減

**v1が動作確認できてから着手すること。**

---

## 10. 使用ライブラリ

### 必須
| ライブラリ | 用途 | インストール |
|---|---|---|
| Pillow (PIL) | 画像処理 | `pip install Pillow` |
| tqdm | 進捗バー | `pip install tqdm` |

### v2で追加
| ライブラリ | 用途 | インストール |
|---|---|---|
| anthropic | Claude API クライアント | `pip install anthropic` |

---

## 11. サンプルデータ（動作確認用）

スクリプトと一緒に作成してください：

### 11.1 テスト画像
- 3枚（簡単な色付き四角形でOK、3:2横）
- `input/` フォルダに配置

### 11.2 size_list.csv サンプル
```csv
商品ID,サイズコード
ITEM001,S
ITEM002,D
ITEM003,Q
```

### 11.3 README.md
以下を含む：
- ツールの概要
- インストール手順
- 使い方
- フォルダ構成の説明
- トラブルシューティング

---

## 12. 実装の進め方（Claude Code への依頼順）

### ステップ 1：全体設計
まず全体のファイル構成と処理の流れを設計してから提示してください。

### ステップ 2：v1 コア実装
1. メイン処理スクリプト（`process.py`）
2. ユーティリティ関数（フォントロード、フォルダ作成など）
3. サンプルデータ（テスト画像、CSV、README）

### ステップ 3：動作確認
サンプルで実行 → エラーがあれば修正

### ステップ 4：実画像でテスト
ユーザーの実画像で動作確認 → 微調整

### ステップ 5：v2（時間があれば）
Vision API統合の依頼を別途行う

---

## 13. ファイル構成（想定）

```
image_processor/
├─ process.py          ← メインスクリプト
├─ utils.py            ← ユーティリティ関数
├─ size_list.csv       ← サイズリスト（ユーザー編集用）
├─ README.md           ← 使い方
├─ requirements.txt    ← ライブラリ一覧
├─ input/              ← 入力画像配置場所
│   └─ (商品画像をここに置く)
└─ samples/            ← サンプルデータ
    ├─ test_image_1.jpg
    ├─ test_image_2.jpg
    ├─ test_image_3.jpg
    └─ size_list_sample.csv
```

---

## 14. 将来の拡張可能性（参考）

時間と興味があれば、以下も検討可能：

- **Web UI化**：Streamlit などでブラウザから操作
- **バッチ実行**：複数シリーズを一度に処理
- **プレビュー機能**：処理前に切り取り結果を確認
- **ECサイト直接アップロード**：Shopify APIなどとの連携
- **画像の自動タグ付け**：Claude Vision APIで色・素材タグを自動付与

---

## 15. 補足：このツールの位置付け

このツールは **「人間のセンスとAIの作業効率を組み合わせる」** ことを目指す：

- **人間が担当**：カメラマンの撮影、構図のセンス、商品企画
- **AIが担当**：100枚の繰り返し処理、フォルダ振り分け、サイズ変換、命名規則

100枚 × 6サイズ = 600枚の手作業（1日仕事）を **コマンド1発で約3分** に短縮することが目標。

---

## 16. ファイル命名ルール（v1.1 追加仕様）

### 16.1 出力ファイル名の基本

**入力ファイル名をそのまま出力ファイル名として使用する**。スクリプト側でリネームやパターン判定は行わず、ユーザーが命名規則を管理する。

| 出力先 | ファイル名 stem | 拡張子 |
|---|---|---|
| 高画質（印刷用・バナー作成用） | 入力 stem そのまま | 元のまま（.jpg / .png） |
| サムネイル・各サイズ・スライダー用 | 入力 stem そのまま | `.webp` に変更 |

例：
- 入力 `flinenblkt01.jpg` → 高画質側 `flinenblkt01.jpg`、WebP側 `flinenblkt01.webp`
- 入力 `cushion-2025.png` → 高画質側 `cushion-2025.png`、WebP側 `cushion-2025.webp`

### 16.2 モデル画像 placeholder 置換

モデル着用画像が含まれる場合のみ働く、軽量な命名補助機能。

#### ユーザー側の命名規約
モデル写真のファイル名には placeholder として `_model_` を含めておく。

例：`flinenblkt01_model_1.jpg`, `flinenblkt01_model_2.jpg`

#### スクリプトの挙動

1. 起動時に `input/` を走査し、ファイル名に `_model_` を含むファイル数を数える
2. **1個以上見つかった場合のみ** 追加で次の質問を出す：

   ```
   モデル画像が N 枚あります。モデル名を入力してください：
   > 花子
   ```

3. 入力されたモデル名で、出力ファイル名の `_model_` 部分を `_model{モデル名}_` に置換

#### 例

| 入力ファイル | モデル名 | 出力（高画質） | 出力（WebP系） |
|---|---|---|---|
| `flinenblkt01_model_1.jpg` | 花子 | `flinenblkt01_model花子_1.jpg` | `flinenblkt01_model花子_1.webp` |

#### スキップ条件
モデル placeholder を含むファイルが 0 枚の場合、この質問は表示しない。

### 16.3 スクリプトが行わないこと

- ファイル名パターンによる自動カテゴリ振り分け
- モデル名以外の placeholder 置換
- 命名規約の検証（ユーザーが正しく命名する責任を持つ）

### 16.4 size_list.csv との関係（v1.2 改訂）

- 「商品ID」は **入力ファイル名 stem（正規化後）と一致** すること
  - 正規化 = 前後空白の除去 + 末尾の「のコピー」「のコピー2」「のコピー (2)」等の除去
- 「新ファイル名」列が記入されている場合、**その値が最終的な出力 stem**（モデル placeholder 置換より優先）
- 「新ファイル名」が空欄なら、§16.2 の `_model_` 置換ルールが適用される
- CSV 検索キーは「正規化済み商品ID」で行う（モデル名・新ファイル名の影響は受けない）

---

## 17. CSV 拡張: 商品IDの正規化と新ファイル名（v1.2 追加仕様）

### 17.1 列構成

```csv
商品ID,新ファイル名,サイズコード
hli_240802_051,mrblkt_navy_1,S
hli_240802_129_2,mrblkt_navy_2,D
hli_260204_163,mrblkt_gray_1,
hli_260204_206,mrblkt_gray_2,Q
```

3 列のうち **必須は「商品ID」のみ**。残り 2 列は独立に空欄可。

| 商品ID にマッチ | 新ファイル名あり | サイズコードあり | 結果 |
|---|---|---|---|
| ✅ | ✅ | ✅ | リネーム + バッジ |
| ✅ | ✅ | ❌ | リネームのみ |
| ✅ | ❌ | ✅ | リネームせずバッジのみ |
| ✅ | ❌ | ❌ | リネームもバッジもなし（CSVにあるだけ）|
| ❌ | — | — | リネームなしバッジなし（既存の挙動） |

### 17.2 商品ID の正規化

`input/` のファイル名 stem に Windows が自動で付ける「**のコピー**」サフィックスがあっても、商品ID 照合では取り除いて一致させる。

| 入力ファイル名 stem | 正規化結果（CSV照合キー） |
|---|---|
| `hli_240802_051` | `hli_240802_051` |
| `hli_240802_051 のコピー` | `hli_240802_051` |
| `hli_240802_051 のコピー2` | `hli_240802_051` |
| `hli_240802_051 のコピー (3)` | `hli_240802_051` |
| `  hli_240802_051  ` | `hli_240802_051` |

CSV 側の「商品ID」も同じルールで正規化されたうえで照合する。

### 17.3 リネームとモデル placeholder の優先順

1. CSV に商品IDが見つかり、「新ファイル名」が空でない → **その値を出力 stem に使う**（モデル置換は適用しない）
2. それ以外で、ファイル名に `_model_` が含まれる → §16.2 の置換を適用
3. どちらでもない → 入力ファイル名 stem をそのまま使う

### 17.4 リネームとバッジは独立

「商品ID」が CSV に存在しても「サイズコード」が空欄ならバッジは付かない。
逆に「新ファイル名」が空欄でも「サイズコード」があればバッジは付く。

### 17.5 後方互換

旧 2 列形式（`商品ID,サイズコード`）の CSV も読める。新ファイル名列が無いとして扱われる（＝リネームなし）。
